let outer : int = 1;
for (let i : int = 0; i < 3; i++) {
    let inner : int = 10;
    while (inner > 0) {
        inner--;
        if (inner == 5) {
            {
                let shadowed : int = 100;
                cout << shadowed;
            }
            continue;
        }
    }
}
